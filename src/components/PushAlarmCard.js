import React from "react";
import { StyleSheet, Text, View } from "react-native";

const PushAlarmCard = () => {
  return (
    <View>
      <Text>Push Alarm Card</Text>
    </View>
  );
};

export default PushAlarmCard;

const styles = StyleSheet.create({});
